image:
  repository: docker.io/estevaofay/cronjob-poc
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "1"

cronjob:
  name: cronjob-poc
  schedule: "* * * * *"
