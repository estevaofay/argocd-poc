apiVersion: v1
kind: Service
metadata:
  name: {{ include "vitor-service.fullname" . }}
  labels:
    {{- include "vitor-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vitor-service.selectorLabels" . | nindent 4 }}
