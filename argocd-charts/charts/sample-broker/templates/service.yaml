apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-broker.fullname" . }}
  labels:
    {{- include "sample-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-broker.selectorLabels" . | nindent 4 }}
