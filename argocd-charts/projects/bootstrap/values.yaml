targetRevision: dev # overriden by kustomize

applications:
  - microservices:
      - name: nginx-ingress
        namespace: nginx-ingress
      - name: cert-manager
        namespace: cert-manager
      - name: external-dns
        namespace: external-dns
      - name: argocd-ingress
        namespace: argocd
      - name: httpbin
        namespace: httpbin
      - name: external-secrets
        namespace: external-secrets
      - name: external-secrets-configuration
        namespace: external-secrets
      - name: fast-api-poc
        namespace: fast-api-poc
